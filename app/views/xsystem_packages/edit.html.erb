<h1>Editing Software</h1>

 
  <%= form_for :system_package , url:[  @system_package.software, @system_package] ,method: :patch do |f| %> 

 <% if @system_package.errors.any? %>
        <div id="error_explanation">
    <h2><%= pluralize(@system_package.errors.count , "error") %> prohibited
      this Program from being saved:</h2>
        <ul>
    <% @system_package.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
<% @soft=Software.find(params[:software_id]) %>
<%= link_to 'Back', [:editDetails , @soft ] %>
