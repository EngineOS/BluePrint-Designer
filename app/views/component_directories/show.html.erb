<%
  back_path = url_for(@component_directory.blueprint_version) + '#' + @component_directory.class.name.underscore + '_' + @component_directory.id.to_s
%>
<%= render 'shared/show/default', record_object: @component_directory, fa: :'caret-right', back_path: back_path %>
