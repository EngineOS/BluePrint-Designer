<div class="col-sm-12">
  <h2><%= content_tag(:i , nil, class: "fa fa-floppy-o") %> Service definitions repository</h2>
  <%= link_to content_tag(:i , nil, class: "fa fa-arrow-left") + " Back",
        service_definitions_path,
        class: 'btn btn-lg btn-warning trigger-response-modal' %>
  <div class="btn-toolbar pull-right">
    <div class="btn-group dropdown pull-right">
      <button type="button" class="btn btn-lg btn-primary dropdown-toggle" data-toggle="dropdown">
        <i class="fa fa-bars"></i>&nbsp<span class="caret"></span>
      </button>
      <ul class="dropdown-menu" role="menu">
        <li>  <%= link_to content_tag(:i , nil, class: "fa fa-warning") + " Reset",
                repository_reset_service_definitions_path,
                class: 'btn-lg',
                data: { confirm: 'Are you sure that you want to reset the service definition repository?', confirm_title: 'Confirm reset' } %></li>
      </ul>
    </div>
  </div>
</div>
<div class="col-sm-12">
  <table class="table table-condensed top-gap">
    <tbody>
      <% Repository::Repository.new.list_service_definition_paths.each do |service_definition| %>
        <tr class="clickable-table-row" href="<%= repository_service_definition_path(id: service_definition.gsub('/', '\\')) %>">
          <td class="trigger-response-modal" style="cursor: pointer;">
            <%= service_definition  %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
