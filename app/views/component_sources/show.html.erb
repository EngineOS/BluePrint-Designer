<%
  back_path = url_for(@component_source.blueprint_version) + '#' + @component_source.class.name.underscore + '_' + @component_source.id.to_s
%>

<%= render 'shared/show/default', record_object: @component_source, fa: :'caret-right', back_path: back_path, nest_in: :blueprint_version, attributes: [[:url, "URL"]] %>
