<%
  back_path = url_for(@persistent_file.blueprint_version) + '#blueprint_version_' + @persistent_file.class.name.underscore + '_' + @persistent_file.id.to_s
%>
<%= render 'shared/show/default', record_object: @persistent_file, fa: :'caret-right', back_path: back_path, nest_in: :blueprint_version %>
