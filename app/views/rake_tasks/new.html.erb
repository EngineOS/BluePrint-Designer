<%
  cancel_path = url_for(@rake_task.blueprint_version) + '#' + @rake_task.class.name.underscore.pluralize
%>
<%= render 'shared/new/default', record_object: @rake_task, fa: :'caret-right', cancel_path: cancel_path %>
