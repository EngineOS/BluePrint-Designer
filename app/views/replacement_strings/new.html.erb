<%
  cancel_path = url_for(@replacement_string.blueprint_version) + '#' + @replacement_string.class.name.underscore.pluralize
%>
<%= render 'shared/new/default', record_object: @replacement_string, fa: :'caret-right', cancel_path: cancel_path %>
