<%
  back_path = url_for(@replacement_string.blueprint_version) + '#blueprint_version_' + @replacement_string.class.name.underscore + '_' + @replacement_string.id.to_s
%>
<%= render 'shared/show/default', record_object: @replacement_string, fa: :'caret-right', back_path: back_path, nest_in: :blueprint_version %>
