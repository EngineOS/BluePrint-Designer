<%
  back_path = url_for(@worker.blueprint_version) + '#blueprint_version_' + @worker.class.name.underscore + '_' + @worker.id.to_s
%>
<%= render 'shared/show/default', record_object: @worker, fa: :'caret-right', back_path: back_path, nest_in: :blueprint_version %>
