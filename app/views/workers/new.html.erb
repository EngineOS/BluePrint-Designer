<%
  cancel_path = url_for(@worker.blueprint_version) + '#' + @worker.class.name.underscore.pluralize
%>
<%= render 'shared/new/default', record_object: @worker, fa: :'caret-right', cancel_path: cancel_path, nest_in: :blueprint_version %>
