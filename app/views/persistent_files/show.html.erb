<%
  back_path = url_for(@persistent_file.blueprint_version) + '#' + @persistent_file.class.name.underscore + '_' + @persistent_file.id.to_s
%>
<%= render 'shared/show/default', record_object: @persistent_file, fa: :'caret-right', back_path: back_path %>
