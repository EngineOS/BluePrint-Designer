<%
  cancel_path = url_for(@persistent_file.blueprint_version) + '#blueprint_version_' + @persistent_file.class.name.underscore + '_' + @persistent_file.id.to_s
%>
<%= render 'shared/edit/default', record_object: @persistent_file, fa: :'caret-right', cancel_path: cancel_path, nest_in: :blueprint_version %>
