<h1>Editing Software</h1>

 
  <%= form_for :softwareservice , url:[  @softwareservice.software, @softwareservice] ,method: :patch do |f| %> 

 <% if @softwareservice.errors.any? %>
        <div id="error_explanation">
    <h2><%= pluralize(@softwareservice.errors.count , "error") %> prohibited
      this Program from being saved:</h2>
        <ul>
    <% @softwareservice.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
   <p>
   	Service:
   	<%= Servicetype.find(@softwareservice.servicetype_id).name %>
   </p>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
<% @soft=Software.find(params[:software_id]) %>
<%= link_to 'Back', [:editDetails , @soft ] %>
