<%
  back_path = url_for(@persistent_directory.blueprint_version) + '#' + @persistent_directory.class.name.underscore + '_' + @persistent_directory.id.to_s
%>
<%= render 'shared/show/default', record_object: @persistent_directory, fa: :'caret-right', back_path: back_path %>
