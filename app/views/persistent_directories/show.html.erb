<%
  back_path = url_for(@persistent_directory.blueprint_version) + '#blueprint_version_' + @persistent_directory.class.name.underscore + '_' + @persistent_directory.id.to_s
%>
<%= render 'shared/show/default', record_object: @persistent_directory, fa: :'caret-right', back_path: back_path, nest_in: :blueprint_version %>
