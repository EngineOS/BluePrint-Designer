<%
  back_path = ['/', @variable.variable_consumer_type.underscore.pluralize,
                  '/', @variable.variable_consumer_id.to_s,
                  "#variable_#{@variable.id}" ].join
%>

<%= render 'shared/show/default', record_object: @variable, fa: :'caret-right', back_path: back_path, nest_in: :blueprint_version %>
