<%
  back_path = url_for(@service_configuration.blueprint_version) + '#' + @service_configuration.class.name.underscore + '_' + @service_configuration.id.to_s
%>
<%= render 'shared/show/default', record_object: @service_configuration, fa: :'caret-right', back_path: back_path %>


  <%= render 'shared/show/association_table', record_object: @service_configuration, association: :variables, fa: :'caret-right', attributes: [:record_label, :label, :value, :status], show_add_button: false, new_item_path: url_for([:new, :variable, variable_consumer_id: @service_configuration.id, variable_consumer_type: :ServiceConfiguration]) %>
