<%
  back_path = url_for(@installed_package.blueprint_version) + '#' + @installed_package.class.name.underscore + '_' + @installed_package.id.to_s
%>
<%= render 'shared/show/default', record_object: @installed_package, fa: :'caret-right', back_path: back_path %>
