<%
  cancel_path = url_for(@port.blueprint_version) + '#' + @port.class.name.underscore + '_' + @port.id.to_s
%>
<%= render 'shared/new/default', record_object: @port, fa: :'caret-right', cancel_path: cancel_path, nest_in: :blueprint_version %>
