<%
  back_path = url_for(@system_package.blueprint_version) + '#blueprint_version_' + @system_package.class.name.underscore + '_' + @system_package.id.to_s
%>
<%= render 'shared/show/default', record_object: @system_package, fa: :'caret-right', back_path: back_path, nest_in: :blueprint_version %>
