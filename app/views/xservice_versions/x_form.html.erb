<%= form_for @service_version do |f| %>
  <%= render 'shared/form/errors', f: f %>
  <%= render 'services/fields', f: f %>
  <%= render 'shared/form/cancel_and_submit_buttons', f: f, cancel_path: @service_version %>
<% end %>